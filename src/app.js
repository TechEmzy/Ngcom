// Import Vue and the CameraApp component
import { createApp } from 'vue';
import CameraApp from './views/SurveyDetails';

// Create the Vue app instance and mount it to the '#app' element
const app = createApp(CameraApp);
app.mount('#app');
